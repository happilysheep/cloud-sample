server:
  port: 8090

#encrypt:
  #key: wangzj


eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone:  'http://localhost:8070/eureka'

spring:
  application:
    name: sample-config
  cloud:
    config:
      server:
        git:
          uri:  'https://gitee.com/wangzj91/config.git'
          clone-on-start: true
      encrypt:
          enabled: false

