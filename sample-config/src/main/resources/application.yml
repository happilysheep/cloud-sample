server:
  port: 8090
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone:  'http://localhost:8070/eureka'

spring:
  application:
    name: sample-config
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: 'file:E:/properties/'

